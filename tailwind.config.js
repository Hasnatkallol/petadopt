module.exports = {
  darkMode: 'class', // you already have this
  theme: {
    extend: {
      colors: {
        primary: 'var(--color-primary)',
      },
    },
  },
  // ... your plugins and content
};
